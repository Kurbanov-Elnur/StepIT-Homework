version: "3.8"
services:
  aspnet-app2:
    build:
      context: ./API Second/
      dockerfile: Dockerfile
    ports:
      - "5002:80"
    networks:
      - app-network

networks:
  app-network:
    driver: overlay